from copy import copy
import dataTypes.cell as ce
import dataTypes.cellNeighbor as cN
import dataTypes.imageState as iS
from dataTypes.dataTypeTraits import cellTraits as cT
from dataTypes.dataTypeTraits import cellNeighborTraits as cNT
from dataTypes.dataTypeTraits import imageStateTraits as iST

mockCell1 = copy(ce.cell)
mockCell2 = copy(ce.cell)
mockCell3 = copy(ce.cell)

mockCellNeighbor1 = copy(cN.cellNeighbor)
mockCellNeighbor2 = copy(cN.cellNeighbor)
mockCellNeighbor3 = copy(cN.cellNeighbor)


def test_cell():
    pass


def test_cellNeighbor():
    pass


def test_imageState():
    pass