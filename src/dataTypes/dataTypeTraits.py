# TODO:: Remove, as this should now be obsolete
from enum import Enum, auto

# The enum is for type completion for my dictionary's keys.



class imageStateTraits(Enum):
    # Images
    IMAGE = auto()
    FILTERED_IMAGE = auto()
    NEIGHBOR_IMAGE = auto()


